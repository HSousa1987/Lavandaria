# Environment Configuration
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=lavandaria
DB_PASSWORD=lavandaria2025
DB_NAME=lavandaria

# Server
PORT=3000

# CORS (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Session (REQUIRED - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=

# File Upload
MAX_FILE_SIZE=5242880
